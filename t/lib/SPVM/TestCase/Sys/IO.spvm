class TestCase::Sys::IO {
  use Sys::IO;
  
  static method fopen : int ($test_dir : string) {
    
    my $file = "$test_dir/ftest/file_empty.txt";
    my $fh = Sys::IO->fopen($file, "rb");
    
    unless ($fh) {
      return 0;
    }
    
    return 1;
  }
  
  
}
